# Extra dependencies for the Unlimited-OCR extraction backend.
# Install on top of requirements.txt:
#     pip install -r requirements.txt -r requirements-unlimited-ocr.txt
#
# Versions below are what Baidu tested (python 3.12.3 + CUDA 12.9). On a CUDA
# box install the matching torch build first, e.g.:
#     pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu129

torch==2.10.0
torchvision==0.25.0
transformers==4.57.1
Pillow==12.1.1
matplotlib==3.10.8
einops==0.8.2
addict==2.4.0
easydict==1.13
pymupdf==1.27.2.2
psutil==7.2.2
accelerate>=1.0.0
