# PostgreSQL Database Settings
DB_HOST=localhost
DB_PORT=5433
DB_NAME=requirements_db
DB_USER=postgres
DB_PASSWORD=postgres  # Replace with the password you set during installation

# Database URL - Direct format for simplicity
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/requirements_db  # Update password here too

# Embedding Model
EMBED_MODEL=all-MiniLM-L6-v2